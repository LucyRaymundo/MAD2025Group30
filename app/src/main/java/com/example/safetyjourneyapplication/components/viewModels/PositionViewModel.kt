package com.example.safetyjourneyapplication.components.viewModels

import androidx.compose.runtime.mutableStateListOf
import androidx.lifecycle.ViewModel
import com.example.safetyjourneyapplication.components.classes.Position
import com.example.safetyjourneyapplication.components.classes.Coordinate
import java.time.LocalDateTime
import java.time.format.DateTimeFormatter


class PositionViewModel : ViewModel() {
    /* var positions =
        mutableStateListOf(
            Position(
                positionID = 1,
                positionActivityID = 1,
                positionActivityName = "Morning Jog",
                positionCoordinate = Coordinate(latitude = 51.509865, longitude = -0.118092),
                positionDatetime = LocalDateTime.parse("2025-03-07T15:30:00", DateTimeFormatter.ISO_LOCAL_DATE_TIME)
            ) ) */
            }
